PROJECT_NAME="SENTINEL AI - CCTV Investigation Platform"
API_V1_STR="/api/v1"
SECRET_KEY="sentinel_secret_key_super_secure_jwt_change_in_production"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database Configuration (PostgreSQL primary with SQLite local fallback)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sentinel_ai"
SQLITE_FALLBACK_URL="sqlite:///./sentinel.db"

# LLM Configuration (Google Gemini / OpenAI / Custom Provider)
GEMINI_API_KEY=""

# File Storage Directory
STORAGE_DIR="./storage"
